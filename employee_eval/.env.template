DB_HOST=localhost # set to hostname/ip of mysql server (ask Jack if you don't know)
DB_PORT=3306 # no change
DB_USER=root # no change
DB_PASSWORD=your_password # set this to the mysqlDB password (again, ask Jack if you don't know)
DB_NAME=employee_performance # no change
DB_CHARSET=utf8mb4 # no change

# Instructions:
# 1. Copy this file and rename it to '.env'
# 2. Replace the placeholder values with your actual database credentials
# 3. Keep your .env file secure and never commit it to version control
